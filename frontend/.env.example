# ====== Frontend Environment Variables ======
# Copy to ".env" for local dev. On Netlify, set these in Site settings -> Environment.
# All frontend vars MUST start with VITE_

# URL of your deployed backend API (Render). For local dev use http://localhost:5000
VITE_API_URL=http://localhost:5000
